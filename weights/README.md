# Model Weights

Place local model weights here, for example:

```text
weights/best.pt
```

Model files are intentionally not committed to this repository. If a trained
model needs to be shared, prefer GitHub Releases or Git LFS.
